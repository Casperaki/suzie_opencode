# lib/

Shared utility functions and configuration.

## Files

- `utils.ts` -- Exports `cn()`, a helper that merges Tailwind classes using `clsx` and `tailwind-merge`. Used by all shadcn/ui components.

## Extending

Add new utility files here for things like API clients, date formatting, validation schemas, or constants. Keep files focused on a single concern.
