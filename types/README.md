# types/

Shared TypeScript type definitions.

## Files

- `index.ts` -- Add custom types here. This is the central place for app-wide type definitions.

## Usage

Import types from `@/types` (the `@/` alias is configured in `tsconfig.json`).

```ts
import type { MyType } from "@/types"
```
